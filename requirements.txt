# Dependencias del microservicio de conciliación bancaria
# Instalar con: pip install -r requirements.txt

flask>=3.0.0
pandas>=2.2.0
openpyxl>=3.1.0  # necesario para generar el reporte .xlsx (bonus Criterio 5)
pytest>=8.0.0
jupyter>=1.0.0